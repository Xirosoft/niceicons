// const inputFile = './src/style/xiroicon/_index.scss';
// const outputFile = './public/font/xiroicon.min.css';

module.exports = {
  plugins: [
    require('autoprefixer'),
    require('cssnano')({
      preset: ['default', {
        discardComments: {
          removeAll: true,
        },
      }]
    })
  ]
};
